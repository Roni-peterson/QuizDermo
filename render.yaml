services:
  - type: web
    name: quizdermo
    env: php
    plan: free
    buildCommand: "composer install --no-dev --optimize-autoloader"
    startCommand: "php artisan serve --host 0.0.0.0 --port 10000"
    envVars:
      - key: APP_ENV
        value: production
      # Adicione aqui outras variáveis, se quiser, ou continue usando o painel do Render